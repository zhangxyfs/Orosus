# provider-anthropic

- **提供**：provider 槽 `provider:anthropic`（`StreamFn`）
- **消费**：无
- **配置**：`[provider-anthropic]` —— `apiKey`（必填，`"$ENV:ANTHROPIC_API_KEY"` 占位）、`baseUrl`（可选）
- **uses**：`network`、`secrets`
